class Solution:
    def reverseOnlyLetters(self, s: str) -> str:
        left = 0
        right = len(s) - 1
        tmp_list = [n for n in s]
        print(tmp_list)
        while True:
            if left >= right:
                break

        return s
